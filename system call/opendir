#include <stdio.h>
#include <dirent.h>

int main(){
    DIR *dir;
    dir = opendir(".");
    if (dir == NULL){
        printf("Failed to open dir\n");
        return 1;
    }
    printf("Dir opened successfully\n");
    closedir(dir);
    return 0;
}
