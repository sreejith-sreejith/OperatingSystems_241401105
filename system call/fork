#include <stdio.h>
#include <unistd.h>

int main() {

    if (fork() == 0) {
        printf("Theis is child process\n");
    }
    else {
        printf("This is parent process\n");
    }

    fork();
    fork();
    fork();

    printf("PID: %d\n", getpid());

    return 0;
}
